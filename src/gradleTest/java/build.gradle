plugins {
    id 'java' // or java-library
    id 'ru.vyarus.quality'
    id 'com.github.spotbugs' version '6.2.5' apply false
}

quality {
    // used to prevent failure due to found quality violations
    strict = false
}

repositories { mavenCentral() }